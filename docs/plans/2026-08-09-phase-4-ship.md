# Phase 4: Ship (Deploy + Demo + Video + Submission)

**Goal:** Public demo URL on AWS Lambda, seeded demo data on CockroachDB Cloud, PRD latency metric re-verified, resilience film, sub-3-minute video, Devpost submission before 2026-08-18 17:00 EDT (target: submit 2026-08-17).
**Prerequisites:** Phases 1-3 complete.
**Estimated scope:** 6 steps, ~1.5 days including video.

---

## Step 1: Lambda entry + bundle + deploy
**Files:** `src/lambda.ts`, `scripts/deploy-lambda.mjs` (or documented CLI commands in `docs/deploy.md`)
**What:** `hono/aws-lambda` adapter around the same app as `src/server.ts`. Bundle with esbuild (single file, ESM, external: none). Deploy via AWS CLI (profile h0, us-east-1): IAM role (Bedrock invoke + logs), function, Function URL (auth NONE, PRD: public demo). Env vars: `ENGRAM_DATABASE_URL` = cloud URL, region, real-Bedrock mode. CRDB Cloud CA cert: commit `certs/crdb-root.crt` (public CA certificate, not a secret) and load it in db.ts when the URL host is *.cockroachlabs.cloud; the SQL password stays ONLY in the Lambda env var, never in the repo.
**Verify:** `curl <function-url>/api/memories?scope_id=demo` returns JSON; `/chat` round-trips with real Bedrock.
**Commit:** `feat: lambda deploy (function url demo)`

## Step 2: Cloud migrate + seed demo data
**Files:** `scripts/seed-demo.mjs`
**What:** Run migrations against the cloud cluster; seed a `demo` scope with a small story arc (10-15 episodic memories over simulated days, 2 sessions of turns, one consolidation run) so the dashboard opens onto something alive: decay curves at different ages, one semantic memory with lineage, recall provenance rows.
**Verify:** dashboard at the Function URL shows the seeded scope end-to-end (browser, curves, lineage, provenance, time-travel).
**Commit:** `feat: demo seed script`

## Step 3: PRD latency metric re-verified on the REAL path
**Files:** `scripts/spike/RESULTS-latency.md`
**What:** PRD success metric: recall p50 < 300ms. Measure the real `recallMemories()` (ANN + re-rank + reinforcement + recall_log write) against the cloud cluster from the Lambda region (run the probe IN Lambda or an ephemeral EC2-free path: a Lambda invoke that times N=50 recalls internally), not the spike table. Record p50/p95.
**Verify:** RESULTS-latency.md committed with numbers; README updated if the metric holds (or honestly noted if it does not).
**Commit:** `docs: real-path recall latency (prd metric)`

## Step 4: Resilience film (local 3-node)
**Files:** `scripts/resilience-demo.ps1`, film clip for the video
**What:** Local 3-node insecure cluster (three cockroach start joined), migrate + seed, run the local server against node 1, drive a chat loop, `Stop-Process` node 2 mid-conversation, show the conversation continuing (retry layer absorbs it), node status before/after. Screen-record the terminal + dashboard. Serverless cannot kill nodes; this is the honest local stand-in and is labelled as such in the video.
**Verify:** clip in hand showing zero dropped requests (withTransientRetry logs retries if any).
**Commit:** `feat: resilience demo script`

## Step 5: Video (sub-3-min)
**What:** Transcript-driven pipeline (script first, then record to the script): 0:00 problem (agents forget; memory is infra), 0:25 live demo: cross-session recall at the Function URL, 1:00 dashboard: decay curves, consolidation lineage, provenance, time-travel slider (AOST vs versions replay shown explicitly), 1:55 resilience clip (node kill), 2:20 architecture card: CRDB tools (vector index EXPLAIN excerpt, managed MCP) + Bedrock, 2:45 close: repo + hippo disclosure line. Record with OBS or the harness-arena Playwright pipeline; captions burned in.
**Verify:** duration < 3:00; every hackathon judging criterion visibly hit; uploaded (YouTube unlisted).

## Step 6: Devpost submission
**What:** Submission form: repo URL (public, MIT), demo URL, video URL, text: what it does, how CRDB tools are meaningfully integrated (vector indexing: two-prefix ANN index with EXPLAIN evidence; managed MCP server: registered + documented introspection; ccloud: ops), AWS services (Bedrock chat + embeddings, Lambda hosting), newly-created statement + hippo disclosure. Submit by Aug 17, buffer day before the Aug 18 deadline.
**Verify:** Devpost shows the submission as complete; all links resolve logged-out.
